{
  "service": {
    "service": "query-local",
    "protocol": "http",
    "host": "localhost:9130",
    "keyFile": "config/key.pem",
    "certFile": "config/cert.pem"
  },
  "mongo": {
    "connectionString": "mongodb://localhost/data"
  },
  "schemaVersion": {
    "minimum": 0,
    "maximum": 2
  }
}
